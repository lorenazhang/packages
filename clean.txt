import pandas as pd
from datetime import date, timedelta
from pathlib import Path
import numpy as np

# =========================
# 1. Auto calculate dates
# =========================

today = date.today()

# 本周周一
this_monday = today - timedelta(days=today.weekday())
call_end_date = this_monday - timedelta(days=7)
start_date = this_monday - timedelta(days=14)
comp_end_date = this_monday

start_date = start_date.strftime("%Y-%m-%d")
call_end_date = call_end_date.strftime("%Y-%m-%d")
comp_end_date = comp_end_date.strftime("%Y-%m-%d")

print("start_date:", start_date)
print("call_end_date:", call_end_date)
print("comp_end_date:", comp_end_date)





#append to tables

target_table = "cdar_cai_prd.eo_conversion_13m_baseline"
#delete if exists

delete_sql = f"""
DELETE FROM {target_table}
WHERE week_start_call = DATE '{start_date}'
"""

cur.execute(delete_sql)
conn_tdap2.commit()

#insert
insert_cols = [
    "week_start_call",
    "calldriver1",
    "primary_lob",
    "sublob",
    "call_count",
    "complaint_count",
    "service_complaint_count",
    "escalated_complaint_count",
    "complaint_conversion_rate",
    "service_conversion_rate",
    "escalated_conversion_rate"
]

df_insert = df_weekly[insert_cols].copy()

# clean numeric values
df_insert = df_insert.replace([np.inf, -np.inf], np.nan)
df_insert = df_insert.where(pd.notnull(df_insert), None)

insert_sql = f"""
INSERT INTO {target_table}
(
    week_start_call,
    calldriver1,
    primary_lob,
    sublob,
    call_count,
    complaint_count,
    service_complaint_count,
    escalated_complaint_count,
    complaint_conversion_rate,
    service_conversion_rate,
    escalated_conversion_rate
)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
"""

cur = conn_tdap2.cursor()
cur.executemany(insert_sql, df_insert.values.tolist())
conn_tdap2.commit()

print(f"Inserted {len(df_insert)} rows into {target_table}")
