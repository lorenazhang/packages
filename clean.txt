
import pandas as pd
from sklearn.metrics import precision_score, recall_score, f1_score

results = pd.DataFrame({
    "method": ["Keyword", "Model"],
    "precision": [
        precision_score(df["ecmp_misconduct"], df["sor_misconduct"], pos_label="yes"),
        precision_score(df["ecmp_misconduct"], df["misconduct_pred"], pos_label="yes")
    ],
    "recall": [
        recall_score(df["ecmp_misconduct"], df["sor_misconduct"], pos_label="yes"),
        recall_score(df["ecmp_misconduct"], df["misconduct_pred"], pos_label="yes")
    ],
    "f1_score": [
        f1_score(df["ecmp_misconduct"], df["sor_misconduct"], pos_label="yes"),
        f1_score(df["ecmp_misconduct"], df["misconduct_pred"], pos_label="yes")
    ]
})

results
