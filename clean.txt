
def contains_gov_or_org_email(text):
    # Regular expression pattern to match emails ending with .gov or .org
    pattern = r'[a-zA-Z0-9._%+-]+@[a-zA-Z0-9.-]+\.(gov|org)\b'
    pattern = r'[a-zA-Z0-9._%+-]+@(occ|fdic)\.(gov|org)\b'

    
    # Search for the pattern in the text
    if re.search(pattern, text):
        return True
    else:
        return False
import re



