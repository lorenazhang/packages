import re

closed_pattern = re.compile(
    r"""\b(
        (unable|cannot|can't|could\s+not|unsuccessful)\s+(to\s+)?(reach|contact|locate|identify|find)
        |
        unable\s+to\s+(send|deliver)\s+(resolution\s+)?letter
        |
        no\s+(response|contact|callback)
        |
        customer\s+(declined|refused|does\s+not\s+want|do\s+not\s+wish)\s+(contact|follow[\s-]?up|outreach)
        |
        unable\s+to\s+(verify|authenticate)\s+customer
        |
        invalid\s+(contact|address|phone|email)
        |
        legal\s+(department|team)\s+(sent|mailed|issued|handled)\s+(the\s+)?(letter|notice|communication)
        |
        letter\s+(sent|issued)\s+by\s+legal
        |
        referred\s+to\s+legal
    )\b""",
    flags=re.IGNORECASE | re.VERBOSE
)
