# Unique queue coverage
unique_match_rate = (
    df['vq_call'].dropna().isin(df_map['vq_map'].dropna()).mean()
)

# Row-level coverage
row_match_rate = df_merge['lob_name'].notna().mean()

print(f"Unique VQ match rate: {unique_match_rate:.2%}")
print(f"Row-level match rate: {row_match_rate:.2%}")


vq_counts = (
    df.groupby('vq_call')
      .size()
      .reset_index(name='record_count')
)

vq_counts['is_mapped'] = vq_counts['vq_call'].isin(df_map['vq_map'])

vq_counts.groupby('is_mapped').agg(
    unique_vq_count=('vq_call', 'nunique'),
    total_records=('record_count', 'sum'),
    median_records_per_vq=('record_count', 'median')
)
